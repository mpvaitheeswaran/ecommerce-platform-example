# ecommerce-platform-example
Ecommerce Platform with microservices and dockerized and also with eureka service registry and spring cloud API Gateway


## Docker compose build
```cmd
cd services
docker compose build
docker compose up
```